import styled from 'styled-components';

const StyledNotFoundPage = styled.div`
  text-align: center;
  margin-top: 20rem;
`;

const NotFoundPageImg = styled.img``;

export { StyledNotFoundPage, NotFoundPageImg };
